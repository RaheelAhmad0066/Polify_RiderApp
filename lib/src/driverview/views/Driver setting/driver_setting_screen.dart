import 'package:flutter/material.dart';

class DriverSettingScreen extends StatelessWidget {
  const DriverSettingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
