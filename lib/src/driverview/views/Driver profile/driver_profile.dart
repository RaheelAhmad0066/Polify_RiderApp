import 'package:flutter/material.dart';

class DriverProfileSetting extends StatelessWidget {
  const DriverProfileSetting({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
