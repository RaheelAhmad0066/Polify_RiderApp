import 'package:flutter/material.dart';

class DriverTermandConditionsScreen extends StatelessWidget {
  const DriverTermandConditionsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
